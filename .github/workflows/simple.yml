name: Sample workflow

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps : 
    - name : step1
      run: node -v 

  run-windows-command:
    runs-on: windows-latest
    steps :
    - name : show location
      run : Get-Location
      